import styles from "./partners.module.scss";

export const Partners = () => {
  return (
    <section className={styles.container}>
      <h1 className={styles.title}>PARTNERS PAGE</h1>
    </section>
  );
};
