import { FC } from "react";
import styles from "./exchangersList.module.scss";

interface ExchangersListProps {}

export const ExchangersList: FC<ExchangersListProps> = (props) => {
  return <section>EXCHANGERS LIST WIDGET</section>;
};
