import { FC } from "react";
import styles from "./selectsBar.module.scss";

interface SelectsBarProps {}

export const SelectsBar: FC<SelectsBarProps> = (props) => {
  return <section>SELECTS BAR WIDGET</section>;
};
