import { Button } from "@/shared/ui";

export const AddReview = () => {
  return (
    <Button variant={"ghost"} className="bg-yellow-main rounded-full text-black px-6 font-semibold">
      ДОБАВИТЬ ОТЗЫВ ОБ ОБМЕННИКЕ
    </Button>
  );
};
